#Install docker
$ ./install-docker.sh

#Check if docker is working
$ docker --version

#Install minikube
$ ./install-minikube.sh

#Check if minikube is working
$ minikube version

#Install kubectl
$ ./install-kubectl.sh

#Check if kubectl is working
$ kubectl version